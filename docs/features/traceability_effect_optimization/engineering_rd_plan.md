# 溯源效果优化研发计划

> 用途：执行文档
> 产品定义：`docs/features/traceability_effect_optimization/product_decision_memo.md`
> 上一阶段成果：`docs/features/precise_traceability/`
> 样本定义：`eval/testsets/evidence_traceability_smoke.json`

---

## 1. 当前判断

“精准溯源”主链路已经完成。当前研发任务不再是打通 `precise/native`，而是提升其证据片段质量，使点击后的首屏体验满足“可秒级确认”的产品要求。

当前约束如下：

- 主链路必须继续保持 `precise/native`
- 不得通过前端 fallback、正文反推或 synthetic/source path 冒充 native precise
- 本阶段聚焦命中类样本 A01、A03、A04、A05
- 验收重点从“链路是否存在”转为“证据片段是否足够干净、聚焦、易确认”

---

## 2. 本阶段目标

本阶段只做 1 件事：

**提升 citation 打开后的证据片段质量，使首屏内容更接近真实依据，而不是读取结果包装。**

将该目标拆为 3 项实施结果：

1. `snippet` 去噪  
   减少 `Assistant`、`User`、XML/标签式包装、工具说明语、无关读取元信息等噪音。

2. 落点收窄  
   让 citation 默认打开位置覆盖 citation 对应的核心 `snippet`，而不是宽泛附近区块。

3. 确认成本下降  
   让用户在打开后无需二次关键词搜索或明显滚动，即可理解证据点所在。

---

## 3. 非目标

- 不重新设计 citation 主链路
- 不改动 `precise/native` 与 `file/*` 的语义边界
- 不新增新的透明度组件
- 不为单个样本定制一次性补丁
- 不把 no-hit 场景纳入本阶段主优化范围

---

## 4. 实施策略

本阶段遵循以下实施顺序：

1. 先治理原生证据片段质量  
   优先检查 `read` 产出的 `source_refs.snippet` 是否包含系统包装信息，必要时在 OpenCode 原生读取结果或事件适配层做清洗与裁剪。

2. 再校准落点逻辑  
   基于 `char_offset + snippet` 校准默认打开区域，确保首屏覆盖 citation 对应的核心依据。

3. 最后做人工走查与 smoke 回归  
   只在 A01、A03、A04、A05 上验证本阶段效果，确认没有因体验优化破坏 native 主路径。

实施原则如下：

- 优先在原生证据层解决，不在应用层制造新的补偿主逻辑
- 优先提升通用片段质量，不为单一题目手写特殊规则
- 优先保证首屏确认效率，不追求更复杂的展示形式

---

## 5. 当前任务看板

| 状态 | 任务 | 当前结果 / 下一步 | 完成标志 |
|---|---|---|---|
| 待开始 | 盘点现有 `snippet` 噪音模式 | 从 A01、A03、A04、A05 的最新 trace 和 UI 首屏内容中归纳包装噪音类型 | 形成明确的噪音清单，可直接指导实现 |
| 待开始 | 定义通用去噪规则 | 将应剔除的包装信息固化为通用规则，避免题目特化 | 生成后的首屏 `snippet` 不再暴露主要系统包装噪音 |
| 待开始 | 校准 citation 默认落点 | 检查 `char_offset + snippet` 对首屏覆盖范围的影响，必要时调整预览裁剪策略 | 打开 citation 后首屏覆盖 citation 对应核心依据 |
| 待开始 | 运行 A01、A03、A04、A05 回归 | 验证主链路仍为 `precise/native`，且效果提升未引入退化 | 4 个样本均满足本阶段工程验收口径 |
| 待开始 | 补充必要回归保护 | 对新增的通用去噪或落点逻辑补最小必要测试 | 后续改动不易回退到包装噪音或宽泛落点 |

---

## 6. 工程验收口径

本阶段研发完成，至少满足以下条件：

1. A01、A03、A04、A05 的最终正文 citation 继续绑定 `precise/native`
2. citation 打开后的首屏默认证据片段不包含以下主要噪音：
   - `Assistant`
   - `User`
   - XML/标签式包装
   - 工具说明语
   - 与当前断言无关的读取元信息
3. 首屏可见区域覆盖 citation 对应的核心 `snippet`
4. 用户无需二次关键词搜索，且无需明显滚动，便可定位到支撑断言的片段
5. 未通过前端 fallback、正文反推 citation 或 synthetic/source path 冒充 native precise

---

## 7. 风险与边界

- 若去噪规则过强，可能损伤真实原文内容，导致证据语义不完整
- 若落点裁剪过窄，可能损失理解当前断言所需的最小上下文
- 若实现位置选错，可能把原生证据优化重新做成应用层补偿
- 若仅凭现有 4 个样本调参，存在对局部样本过拟合的风险

本阶段应优先选择：

- 可解释的通用规则
- 最小必要的上下文保留
- 能被后续 smoke 与人工走查持续验证的实现方式

---

## 8. 维护方式

- 仅在目标、任务看板、工程验收口径发生变化时更新
- 新发现的噪音模式优先补入“通用去噪规则”范围，不单独扩展文档叙事
- 当验收流程独立发展为完整 checklist 或样本矩阵时，再拆分单独的 validation 文档
