# 跳跃游戏III题解

## 🧑‍💻 User

讲解力扣题目1306. 跳跃游戏 III

---
## 🤖 Assistant

题目：Leet码 1306. 跳跃游戏 III（Jump Game III）

---

## 一、题目本质（用你熟悉的话理解）

给你一个数组 `arr` 和一个起始下标 `start`。

- 你现在站在下标 `start`。
- 在位置 `i` 上，你可以跳到：
 - `i + arr[i]`
 - `i - arr[i]`
- 问：**能不能跳到某个位置 `j`，使得 `arr[j] == 0`？**

本质上是一个：
> 从起点 `start` 出发，在一个“隐式图”里，能不能走到某个值为 0 的节点？

把每个下标看成一个“节点”，从 `i` 能走到 `i + arr[i]` 和 `i - arr[i]`（如果没越界）。

---

## 二、核心思路（图搜索：BFS 或 DFS）

### 1. 建模成图

- **节点**：数组下标 `0 ~ n-1`
- **边**：
 - 从 `i` 到 `i + arr[i]`（如果在范围内）
 - 从 `i` 到 `i - arr[i]`（如果在范围内）
- 问：从 `start` 出发，能不能到达一个节点 `j`，使得 `arr[j] == 0`？

这是最标准的“图的可达性”问题，直接用 **BFS / DFS + visited 数组**。

### 2. 为什么要用 visited？

数组可能存在“环形跳转”，比如：

- `arr = [2, 3, 1, 1, 4]`
- 某些下标可能互相跳来跳去：`1 -> 4 -> 1`

如果你不记录“访问过的下标”，你会在这种环里 **死循环**：
- 1 -> 4 -> 1 -> 4 -> ... 永远停不下来

所以必须用一个 `visited` 数组／集合，保证：
- 每个下标最多处理一次
- 时间复杂度是 O(n)，不会超时

### 3. BFS 步骤（推荐）

用队列实现 BFS，流程：

1. 初始化队列 `queue`，把 `start` 放进去
2. `visited[start] = True`
3. 循环：
 - 从队列里弹出当前下标 `i`
 - 如果 `arr[i] == 0`，返回 True
 - 计算两个下一跳：
 - `next1 = i + arr[i]`
 - `next2 = i - arr[i]`
 - 对每个 `next`：
 - 如果在边界内且没访问过：
 - 标记 `visited[next] = True`
 - 入队
4. 队列空了还没找到 0，返回 False

复杂度：
- **时间**：O(n)，每个下标最多入队一次
- **空间**：O(n)，visited + 队列

DFS 也可以，但 Python 递归深度可能不安全，面试里说 BFS/DFS 任意一个都行即可。

---

## 三、代码实现（Python，Google 风格）

```python
from collections import deque
from typing import List


class Solution:
    def canReach(self, arr: List[int], start: int) -> bool:
        """判断是否可以从 start 跳到值为 0 的位置.

        思路:
            把每个下标看成图中的一个节点, 从 i 可以跳到
            i + arr[i] 和 i - arr[i] (如果没有越界). 问题就
            变成: 从 start 出发, 是否能到达某个 arr[j] == 0 的节点.

            使用 BFS (或 DFS) 搜索, 加一个 visited 数组避免死循环。
            每个下标最多访问一次, 时间复杂度 O(n).

        Args:
            arr: 输入数组, arr[i] 表示从 i 能跳跃的步数.
            start: 起始下标.

        Returns:
            bool: 如果可以跳到某个值为 0 的下标, 返回 True,
            否则返回 False.
        """
        n = len(arr)
        if arr[start] == 0:
            # 起点就是 0, 直接返回 True
            return True

        visited = [False] * n
        queue = deque()
        queue.append(start)
        visited[start] = True

        while queue:
            idx = queue.popleft()

            # 如果当前下标的值为 0, 找到了
            if arr[idx] == 0:
                return True

            # 计算下一步可能跳到的两个位置
            for next_idx in (idx + arr[idx], idx - arr[idx]):
                # 判断是否在数组范围内且未访问过
                if 0 <= next_idx < n and not visited[next_idx]:
                    visited[next_idx] = True
                    queue.append(next_idx)

        # 遍历完所有可达位置仍未找到 0
        return False
```

---

## 四、为什么这个解法是“对的”

1. **覆盖所有可能路径**
BFS/DFS 会从 `start` 出发，把所有“从这里能跳到的合法下标”都探索一遍，不会漏掉任何一种跳法。

2. **不会死循环**
`visited` 确保每个下标最多进队一次，即使存在环，也只会走一圈，不会无限跳。

3. **复杂度满足要求**
`arr.length` 最大 5 * 10^4，O(n) 的 BFS/DFS 完全没问题。

---

## 五、面试时 30 秒口述思路（可以背）

> 这题我把它看成一个图的可达性问题：数组每个下标是一个节点，从 i 可以跳到 i + arr[i] 和 i - arr[i]（如果不越界）。我从 start 做一次 BFS 或 DFS 搜索，看能不能到达某个值为 0 的下标。为了避免在环里死循环，我用一个 visited 数组记录哪些下标已经访问过，每个下标最多访问一次，所以时间复杂度是 O(n)，空间复杂度也是 O(n)。搜索过程中只要遇到 arr[i] == 0 就直接返回 True，最后搜索完没找到就返回 False。

---
## 🤖 Assistant

好的，没问题。作为一名有经验的工程师，你的强项在于抽象问题和系统设计能力。学习算法的关键是**将抽象问题和你熟悉的模型对应起来**，而不是死记硬背。对于这道题，我会帮你建立这种思维连接。

### LeetCode 1306. 跳跃游戏 III

#### 1. 问题分析 (第一感觉)

拿到这道题，我们先看清目标和规则：
* **目标**：判断能否从 `start` 下标到达**任意一个**值为 0 的下标。
* **规则**：在下标 `i`，可以跳到 `i + arr[i]` 或 `i - arr[i]`。
* **限制**：不能跳出数组边界。

读完后，你脑海里应该浮现一个画面：你站在一个点（`start`），有两个选择可以去下一个点。到了下一个点，又有新的选择...... 这像什么？**这其实就是一个“寻路”问题**。

在计算机科学里，寻路问题最经典的模型就是**图（Graph）**。

* **图的节点 (Node)**：数组中的每一个下标（0, 1, 2, ...）。
* **图的边 (Edge)**：从一个下标 `i` 到另一个下标 `j` 的“跳跃”关系。比如，从 `i` 可以跳到 `i + arr[i]`，那么就有一条从 `i` 指向 `i + arr[i]` 的有向边。

所以，这个问题被我们成功地转化成了一个更熟悉的问题：**在一个图中，从一个起始节点出发，看能不能到达任何一个“目标节点”（值为0的节点）**。

#### 2. 核心思路：图的遍历

一旦确定是图的问题，那么解决“能否到达”这个问题的标准方法就是**图的遍历**。最常用的两种遍历方法是：

1. **广度优先搜索 (BFS - Breadth-First Search)**
2. **深度优先搜索 (DFS - Depth-First Search)**

对于这道题，两者都可以，我们优先选择广度优先搜索（BFS），因为它更符合我们一层一层向外探索的直觉。

**为什么要用 BFS？**
BFS 就像往水里扔一块石头，波纹会一圈一圈地向外扩散。我们从 `start` 这个点开始，先访问所有一步能到达的点，再访问所有两步能到达的点... 这个过程能保证我们不漏掉任何一个可以到达的点。

**BFS 的关键要素：**
1. **一个队列 (Queue)**：用来存放“下一步要去访问”的节点。就像你的待办事项列表，先来的先处理。
2. **一个集合 (Set) 来记录已访问过的节点 (visited)**：这是**至关重要**的一步！想象一下，如果你从下标 3 跳到下标 5，又从下标 5 跳回了下标 3，如果你不记录下来，就会陷入“3 -> 5 -> 3 -> 5 ...”的无限循环中。`visited` 集合就是为了防止这种情况。

**BFS 过程分解：**
1. 把起点 `start` 放入队列，并标记为“已访问”。
2. 当队列不为空时，循环执行：
 a. 从队列头部取出一个节点（当前下标 `curr_index`）。
 b. **检查是否到达终点**：判断 `arr[curr_index]` 是否为 0，如果是，说明找到了，直接返回 `True`。
 c. **寻找下一个节点**：计算两个可能的下个位置 `next_index1 = curr_index + arr[curr_index]` 和 `next_index2 = curr_index - arr[curr_index]`。
 d. **将有效的新节点入队**：对于每一个 `next_index`，如果它在数组范围内，并且**从未被访问过**，就把它加入队列，并标记为“已访问”。
3. 如果队列都空了，循环结束了，还没找到值为 0 的节点，说明从 `start` 出发无法到达任何目标，返回 `False`。

#### 3. 面试口述思路要点 (30秒版本)

> “面试官好，对于这道题，我的思路是把它看作一个图的搜索问题。数组的每个下标可以看作一个图的节点，跳跃规则就是节点之间的边。
>
> 我会采用广度优先搜索（BFS）来解决。我会用一个队列来存储待访问的下标，并用一个 `visited` 集合来记录已访问过的下标，防止陷入死循环。
>
> 具体来说，从 `start` 下标开始，将它入队。然后循环取出队首元素，检查其值是否为0。如果不是，就计算它能跳到的下一个合法且未访问过的位置，并将这些新位置加入队列。
>
> 如果在队列为空之前找到了值为0的下标，就返回 `True`，否则返回 `False`。”

#### 4. Python 代码实现

这里使用 `collections.deque` 作为队列，它的头部删除操作 (`popleft`) 效率是 O(1)，比普通 list 的 O(n) 更高效。

```python
import collections
from typing import List

def can_reach(arr: List[int], start: int) -> bool:
    """
    判断是否可以从 start 下标到达值为 0 的下标。

    使用广度优先搜索 (BFS) 解决。
    """
    # 如果起始位置的值就是0，直接返回True
    if arr[start] == 0:
        return True

    n = len(arr)
    # 1. 初始化队列，并将起始点放进去
    queue = collections.deque([start])
    # 2. 初始化 visited 集合，记录访问过的下标，防止死循环
    visited = {start}

    # 3. 当队列不为空时，持续搜索
    while queue:
        # 从队列左侧取出一个下标
        curr_index = queue.popleft()

        # 计算下一步的两个可能位置
        # 向右跳
        next_index_right = curr_index + arr[curr_index]
        # 向左跳
        next_index_left = curr_index - arr[curr_index]

        # 处理向右跳的情况
        if 0 <= next_index_right < n and next_index_right not in visited:
            # 检查是否到达目标
            if arr[next_index_right] == 0:
                return True
            # 如果不是目标，就加入待访问队列和已访问集合
            queue.append(next_index_right)
            visited.add(next_index_right)

        # 处理向左跳的情况
        if 0 <= next_index_left < n and next_index_left not in visited:
            # 检查是否到达目标
            if arr[next_index_left] == 0:
                return True
            # 如果不是目标，就加入待访问队列和已访问集合
            queue.append(next_index_left)
            visited.add(next_index_left)
    
    # 4. 如果队列都空了还没找到，说明不可达
    return False

# 示例
arr = [4,2,3,0,3,1,2]
start = 5
print(f"从索引 {start} 开始，能否到达值为 0 的位置? {can_reach(arr, start)}")

arr = [4,2,3,0,3,1,2]
start = 0
print(f"从索引 {start} 开始，能否到达值为 0 的位置? {can_reach(arr, start)}")

arr = [3,0,2,1,2]
start = 2
print(f"从索引 {start} 开始，能否到达值为 0 的位置? {can_reach(arr, start)}")
```

### 总结

对于你这样的资深工程师，最快的学习路径就是：
1. **识别问题模型**：看到“能否到达”、“最短路径”等，立刻联想到**图搜索**。
2. **套用标准模板**：图搜索问题，就用 BFS 或 DFS。BFS 有队列和 visited 集合，DFS 有递归和 visited 集合。
3. **关注边界条件**：数组越界、重复访问（死循环）是这类问题最常见的坑，`visited` 集合就是专门用来填这个坑的。

这道题是图搜索的入门经典题，理解透了它，很多类似的题目（如岛屿数量、迷宫问题）的核心思路都是相通的。祝你面试准备顺利！
